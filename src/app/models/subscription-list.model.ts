export interface SubscriptionListElement {
    id: string;
    userId: string;
    contractCode: string;
    startDate: string;
    endDate: string;
    monthlyAmount: number;
    promoCode: string;
    status: string;
}